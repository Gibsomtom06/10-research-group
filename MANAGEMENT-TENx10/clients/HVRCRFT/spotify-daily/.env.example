# Spotify Web API credentials for HVRCRFT daily autopull.
# Create an app at https://developer.spotify.com/dashboard
# (no redirect URL needed — uses client_credentials flow for public data)
#
# Drop the real values either:
#   1. Here as .env (gitignored), OR
#   2. In the umbrella-root .env at "10 Research Group/.env"
#
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
