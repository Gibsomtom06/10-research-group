# DirtySnatcha Release References

Pointers to where DirtySnatcha's recorded music actually lives. No duplicate files here.

| Release | Label | Canonical location |
|---|---|---|
| (populate from contracts/metadata) | DSR | `../../../../DirtySnatcha Records/releases/<folder>/` |
| (populate) | Circus Records | external — see `circus-records-fx.pdf` in mgmt-contracts/ |

Update this table as releases are added. TENx10 can auto-generate this from the Supabase `artist_releases` table once data is loaded.
